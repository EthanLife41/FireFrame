[pytest]
testpaths = tests
# Starlette's TestClient can trigger an httpx DeprecationWarning about the 'app'
# shortcut. It comes from the framework, not this project, so keep it out of the
# test output.
filterwarnings =
    ignore:The 'app' shortcut is now deprecated:DeprecationWarning
