.env
backend/config.py

.venv/
venv/
__pycache__/
*.pyc
.DS_Store
logs/
*.log
.server.pid

# Local calendar exports — never commit private calendar data
*.ics

# Project-local tool binaries (e.g. blueutil) — don't commit binaries
bin/

# Ignore everything in photos/ except the README (no personal images in git)
photos/*
!photos/README.md
