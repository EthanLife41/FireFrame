# Photos

Put `.jpg`, `.jpeg`, `.png`, `.gif`, or `.webp` files here and they show up in the Photos tab.

Personal photos are gitignored (everything in this folder except this README), so they are never committed. The tablet does not need to sign into iCloud or Google Photos. To keep pictures fully outside the repo, set `PHOTOS_DIR` in `.env` to a folder elsewhere on the Mac.
